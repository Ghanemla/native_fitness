import { StyleSheet, Text, Image, View } from 'react-native';

export default function ScreenFooter() {
	return (
		<View style={{backgroundColor: "#aaf"}}>
			<Text>Footer</Text>
		</View>
	);
}

const styles = StyleSheet.create({
	navIcon: {
		fontSize: 24
	},
});
